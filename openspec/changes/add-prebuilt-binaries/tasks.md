## 1. CLI: `--version` flag

- [x] 1.1 In `cmd/explorer/main.go`, add three package-level `var` declarations: `version = "dev"`, `commit = "unknown"`, `buildDate = "unknown"`. Place them next to the existing `playgroundBanner` constant.
- [x] 1.2 Register `--version` and the `-v` alias on the `flag.NewFlagSet` instance. Use `fs.Bool("version", false, "Print version information and exit")` plus a second `fs.BoolVar(...)` registration for `"v"` pointing at the same backing variable so both flags share state.
- [x] 1.3 Update the `Usage` callback to include `[--version]` in the usage line and ensure the flag-defaults output documents both `--version` and `-v`. Do NOT change the existing playground footer text or its position.
- [x] 1.4 After `fs.Parse(flagArgs)` returns successfully, check the `--version` boolean **before** the directory positional validation. If true, print exactly `fmt.Printf("%s (commit %s, built %s)\n", version, commit, buildDate)` to stdout and `os.Exit(0)`. The version path SHALL NOT print the playground banner or start an HTTP server.
- [x] 1.5 Verify by hand: `go run ./cmd/explorer --version` prints `dev (commit unknown, built unknown)` and exits 0; `go run ./cmd/explorer -v` does the same; `go run ./cmd/explorer ./testdata --version` also prints the version line and exits 0 without binding a port.

## 2. CLI: tests for `--version`

These tests SHALL follow the existing subprocess pattern in `cmd/explorer/cli_test.go` (`package main`, `buildTestBinary` + `runOnce` helpers). Do NOT refactor `main` into a `run(args, stdout, stderr) int` function — the existing tests already exercise the binary via subprocess and adding a `run()` indirection would conflict with that pattern.

- [x] 2.1 Add `TestVersionFlag` to `cmd/explorer/cli_test.go`: call `runOnce(t, "--version")`, assert exit code 0, assert stdout equals `"dev (commit unknown, built unknown)\n"` (the locally-built defaults), and assert stderr is empty.
- [x] 2.2 Add `TestVersionShortAlias`: call `runOnce(t, "-v")` and assert stdout matches `TestVersionFlag`'s stdout exactly. Confirms the alias shares state.
- [x] 2.3 Add `TestVersionShortCircuitsMissingDir`: `runOnce(t, "--version")` (no positional arg) MUST return exit code 0 — `--version` takes precedence over the existing missing-directory error path. (`TestMissingDirArgPrintsFooter` already covers the no-version case.)
- [x] 2.4 Add `TestVersionCombinedWithFlagsAndDir`: `runOnce(t, t.TempDir(), "--port", "0", "--version")` MUST return exit 0, stdout is the version line, and `runServerUntilURL` is NOT called (no server start). The 5-second `runOnce` timeout will catch a regression where the binary actually starts serving.
- [x] 2.5 Add `TestVersionLdflagsInjection`: in the test, do a one-off `go build -ldflags "-X main.version=v9.9.9-test -X main.commit=deadbee -X main.buildDate=2026-05-15" -o <tempbin> .` (do not reuse `buildTestBinary` — it caches a no-ldflags build via `sync.Once`). Exec `<tempbin> --version` and assert stdout is exactly `"v9.9.9-test (commit deadbee, built 2026-05-15)\n"`.
- [x] 2.6 Run `make test` and confirm all existing tests still pass (especially `TestPlaygroundBannerLoopback`, `TestPlaygroundBannerNonLoopback`, `TestMissingDirArgPrintsFooter`, `TestHelpFlagPrintsFooter` in `cli_test.go`, and `TestSmokeEndpoint` in `main_test.go`).

## 3. Makefile: release-snapshot and checksums targets

- [x] 3.1 Add a `release-snapshot` target to `Makefile`. Produce **both shapes** per target — archive bundle and bare binary — sharing the `explorer_snapshot_<os>_<arch>` filename prefix. The in-archive entry name MUST be `explorer` (or `explorer.exe` for windows), NOT the per-target snapshot name — so build into a per-target staging directory and archive from there. Steps:
  - (a) `rm -rf dist && mkdir -p dist`
  - (b) `make web` once
  - (c) For each `(GOOS, GOARCH)` pair, create a staging dir `dist/staging/<os>_<arch>/`, then `GOOS=<os> GOARCH=<arch> go build -trimpath -ldflags "-s -w -X main.version=snapshot -X main.commit=$$(git rev-parse --short HEAD) -X main.buildDate=$$(date -u +%Y-%m-%d)" -o dist/staging/<os>_<arch>/explorer<ext> ./cmd/explorer` (where `<ext>` is `.exe` for windows, empty otherwise).
  - (d) Copy `LICENSE` and `README.md` into each staging dir.
  - (e) From inside each staging dir, create the archive in `dist/`: linux/darwin → `tar -C dist/staging/<os>_<arch> -czf dist/explorer_snapshot_<os>_<arch>.tar.gz explorer LICENSE README.md`; windows → `cd dist/staging/<os>_<arch> && zip ../../explorer_snapshot_<os>_<arch>.zip explorer.exe LICENSE README.md && cd -`. The `tar -C` and `cd && zip` forms guarantee no nested directory in the archive (the in-archive entries are bare `explorer`, `LICENSE`, `README.md`).
  - (f) Copy the bare binary alongside the archive: `cp dist/staging/<os>_<arch>/explorer<ext> dist/explorer_snapshot_<os>_<arch><ext>` (where `<ext>` is `.exe` for windows, empty otherwise). The bare binary is byte-identical to the archive's `explorer[.exe]` entry — both are the same `go build` output.
  - (g) `rm -rf dist/staging` once all assets are produced, leaving exactly twelve files (six archives + six bare binaries) in `dist/`.
- [x] 3.2 Add a `checksums` target that runs `cd dist && sha256sum explorer_snapshot_* > checksums.txt` (use `shasum -a 256` if `sha256sum` is unavailable on the local OS — detect with `command -v sha256sum`). The `explorer_snapshot_*` glob covers both archives and bare binaries (shared prefix), naturally excludes `checksums.txt` itself, and SHALL produce exactly twelve lines in the canonical `sha256sum` format (two spaces between hash and filename).
- [x] 3.3 Update the `clean` target to also `rm -rf dist`.
- [x] 3.4 Append `dist/` to the existing `.gitignore` under a new `# Release archives` comment block (the file exists with structured comment groups for "Go build output", "Web dependencies", "IDE", "OS", etc. — preserve that style; do NOT recreate the file).
- [x] 3.5 Verify locally: `make release-snapshot && make checksums && ls dist/` shows twelve assets (six archives + six bare binaries) plus `checksums.txt`. Extract `dist/explorer_snapshot_linux_amd64.tar.gz` to a temp dir with `tar -xzf` and confirm the temp dir contains exactly `explorer`, `LICENSE`, `README.md` at top level (no nested directory); `./explorer --version` from the extracted dir SHALL print `snapshot (commit <short-sha>, built <yyyy-mm-dd>)`. Then run `chmod +x dist/explorer_snapshot_linux_amd64 && dist/explorer_snapshot_linux_amd64 --version` and confirm output is identical (proves the bare binary matches the archive entry). Finally, `cd dist && sha256sum -c checksums.txt` SHALL report `OK` for all twelve files. **Note: verified on linux/arm64 (sandbox arch) since the sandbox is arm64; layout was verified on linux/amd64 archive.**

## 4. GitHub Actions: release workflow

- [x] 4.1 Create `.github/workflows/release.yml`. Set `on.push.tags: ['v*.*.*']`. Add a top-level `permissions: contents: write` so the workflow can create a GitHub Release.
- [x] 4.2 Define a `build-web` job on `ubuntu-latest`: checkout the repo at the tag, set up Node via `actions/setup-node@v4` with `node-version: '20'` (current LTS — `web/.nvmrc` does not exist and `web/package.json` declares no `engines.node`, so pin in the workflow), then `cd web && npm ci && npm run build`, then upload `internal/server/ui/dist/` as a workflow artifact named `spa-dist`. Optionally: as a follow-up cleanup, add `web/.nvmrc` so the workflow and local dev share a single source of truth — out of scope for this change.
- [x] 4.3 Define a `build-binaries` job that depends on `build-web`, runs on `ubuntu-latest`, and uses `strategy.matrix` with the six `{goos, goarch}` pairs (linux/amd64, linux/arm64, darwin/amd64, darwin/arm64, windows/amd64, windows/arm64). Steps: checkout, set up Go via `actions/setup-go@v5` (Go 1.24+), download the `spa-dist` artifact into `internal/server/ui/dist/`. Derive build vars in a `run:` step using GH Actions context — `VERSION=${{ github.ref_name }}`, `SHORT_SHA=$(git rev-parse --short=7 HEAD)` (explicit `--short=7` pin to match the spec's "first 7 characters of the tagged commit's SHA" requirement; git's default short length is usually 7 but is not guaranteed across configurations), `BUILD_DATE=$(date -u +%Y-%m-%d)` — and export them to `$GITHUB_ENV` so subsequent steps see them. The build step uses GH Actions interpolation for matrix values: `GOOS=${{ matrix.goos }} GOARCH=${{ matrix.goarch }} go build -trimpath -ldflags "-s -w -X main.version=$VERSION -X main.commit=$SHORT_SHA -X main.buildDate=$BUILD_DATE" -o dist/staging/explorer$( [ "${{ matrix.goos }}" = "windows" ] && echo .exe ) ./cmd/explorer`. **Implementation note: vars derived once in a small `prepare` job and exposed as outputs to all downstream jobs (build-binaries / smoke-test / release), so BUILD_DATE cannot drift across a midnight-UTC boundary mid-workflow. All matrix entries use the same VERSION/SHORT_SHA/BUILD_DATE.**
- [x] 4.4 Within the same matrix job, after the build:
  - (a) Copy `LICENSE` and `README.md` into `dist/staging/`, then package the archive: linux/darwin → `tar -C dist/staging -czf dist/explorer_${VERSION}_${{ matrix.goos }}_${{ matrix.goarch }}.tar.gz explorer LICENSE README.md`; windows → `cd dist/staging && zip ../explorer_${VERSION}_windows_${{ matrix.goarch }}.zip explorer.exe LICENSE README.md && cd ../..`. Both forms produce archives whose top-level entries are `explorer[.exe]`, `LICENSE`, `README.md` with no nested directory.
  - (b) Copy the bare binary alongside the archive: linux/darwin → `cp dist/staging/explorer dist/explorer_${VERSION}_${{ matrix.goos }}_${{ matrix.goarch }}`; windows → `cp dist/staging/explorer.exe dist/explorer_${VERSION}_windows_${{ matrix.goarch }}.exe`. The bare binary is byte-identical to the archive's binary entry (same `go build` output).
  - (c) Upload both files as a single per-matrix workflow artifact containing only the two release-bound assets in `dist/` — the archive and the bare binary — not the `dist/staging/` directory (artifact name: `assets-${{ matrix.goos }}-${{ matrix.goarch }}`).
- [x] 4.5 Add a `smoke-test` job that depends on `build-binaries`, runs on `ubuntu-latest`, downloads all six per-matrix asset artifacts, and runs:
  - **Execution smoke test (linux/amd64).** Take the bare binary `dist/explorer_${VERSION}_linux_amd64` directly (no extract step), `chmod +x` it, then run `./dist/explorer_${VERSION}_linux_amd64 --version` and assert the line equals `${VERSION} (commit ${SHORT_SHA}, built ${BUILD_DATE})` exactly. Start it as `./dist/explorer_${VERSION}_linux_amd64 ./testdata --port 18080` in the background, wait for the port to be listening (poll `curl --fail-with-body --silent --show-error http://127.0.0.1:18080/` up to 5s with a `until ... do sleep 0.2; done` loop), assert HTTP 200 (curl `--fail`) with `Content-Type: text/html` (check via `-I` HEAD or `-w '%{content_type}'`) and a non-empty body, then send SIGTERM (or SIGINT) and assert the process exits 0 within the graceful-shutdown timeout. On an empty embed the response would be HTTP 404 with `Content-Type: text/plain` and body `404 page not found` (the `http.ServeFileFS` fallback for missing `index.html`); the assertions correctly fail in that case.
  - **Execution smoke test (linux/arm64).** Install QEMU user-mode (`sudo apt-get update && sudo apt-get install -y qemu-user-static`) and repeat the `--version` and `GET /` checks via `qemu-aarch64-static ./dist/explorer_${VERSION}_linux_arm64 ...`.
  - **Structural archive assertion (all six targets).** For each archive (`dist/explorer_${VERSION}_*.tar.gz` and `dist/explorer_${VERSION}_*.zip`), list contents (linux/darwin → `tar -tzf <archive> | sort`; windows → `unzip -l <archive>`) and assert exactly three top-level entries: `explorer` (or `explorer.exe`), `LICENSE`, `README.md`. Fail the job if any archive has a nested directory or extra files. This step covers darwin and windows targets that the execution smoke test cannot.
- [x] 4.6 Add a `release` job that depends on `smoke-test`, downloads all six per-matrix asset artifacts into one directory (yielding twelve binary release files: six archives + six bare binaries), generates `checksums.txt` via `sha256sum explorer_${VERSION}_* > checksums.txt` (in the directory containing the assets — the shared prefix covers both shapes), and uses `softprops/action-gh-release@v2` to create a GitHub Release at the tag with all twelve assets plus `checksums.txt` attached. Set `fail_on_unmatched_files: true`. Mark `make_latest: true` if the tag does not contain `-rc`/`-beta`/`-alpha` (use a small shell step to compute this and pass it through `$GITHUB_OUTPUT`). Consider pinning `softprops/action-gh-release` to a commit SHA instead of `@v2` for supply-chain hygiene — note this trade-off in a code comment so a future maintainer knows why.
- [x] 4.7 Verify the workflow file with `actionlint` if available locally (`actionlint .github/workflows/release.yml`); otherwise rely on GitHub's syntax check at push time. **actionlint not available in dev sandbox; relying on GitHub's syntax check at push time.**

## 4a. GitHub Actions: PR test builds

The PR workflow shares most of its shape with `release.yml` (same matrix, same `build-web` + `build-binaries` + smoke-test logic). The differences are: trigger (`pull_request` instead of tag push), version derivation (`v0.0.0-${SHORT_SHA}` instead of the tag), asset destination (workflow artifacts instead of GitHub Release), permissions (`contents: read` instead of `contents: write`), and the absence of a `release` job. ~50 lines of duplication is accepted; if it grows, refactor to a reusable `workflow_call` workflow.

- [x] 4a.1 Create `.github/workflows/pr-build.yml`. Set `on: pull_request:` (no `types:` override — the default `opened`, `synchronize`, `reopened` covers what we need). Add top-level `permissions: contents: read` (no other scopes — explicit narrowing in case repo-default permissions ever change). Add a top-level `concurrency:` block: `group: pr-build-${{ github.event.pull_request.number }}` with `cancel-in-progress: true`.
- [x] 4a.2 Define a `build-web` job identical in shape to `release.yml`'s 4.2: `ubuntu-latest`, checkout the PR head, `actions/setup-node@v4` with `node-version: '20'`, `cd web && npm ci && npm run build`, upload `internal/server/ui/dist/` as artifact `spa-dist`. Same step body — copy from `release.yml` rather than parameterising; if duplication later becomes painful, extract to a `workflow_call` workflow then.
- [x] 4a.3 Define a `build-binaries` job that depends on `build-web`, runs on `ubuntu-latest`, uses the same six-target `strategy.matrix` as `release.yml` 4.3. The version derivation differs: in a `run:` step set `SHORT_SHA=$(git rev-parse --short=7 HEAD)`, `VERSION=v0.0.0-${SHORT_SHA}`, `BUILD_DATE=$(date -u +%Y-%m-%d)`, and export to `$GITHUB_ENV`. The `go build` invocation is otherwise identical to `release.yml`'s (same `-ldflags "-s -w -X main.version=$VERSION -X main.commit=$SHORT_SHA -X main.buildDate=$BUILD_DATE"`, same staging dir at `dist/staging/explorer$([ "${{ matrix.goos }}" = "windows" ] && echo .exe)`). **Same prepare-job approach as release.yml — vars derived once and exposed as outputs. Each checkout step uses `ref: ${{ github.event.pull_request.head.sha }}` so `git rev-parse HEAD` returns the head SHA (not the synthetic merge SHA).**
- [x] 4a.4 Same packaging step as `release.yml` 4.4: archive the staging dir (`tar.gz` for unix, `zip` for windows) plus copy the bare binary alongside, sharing the `explorer_${VERSION}_<os>_<arch>` prefix. Upload both files as a per-matrix workflow artifact named `assets-${{ matrix.goos }}-${{ matrix.goarch }}` via `actions/upload-artifact@v4`. Do NOT override `retention-days` — accept the repo/org default (typically 90 days). Pin `actions/upload-artifact` to `@v4` (or a commit SHA — match whatever supply-chain policy the release workflow uses).
- [x] 4a.5 Add a `smoke-test` job that depends on `build-binaries`, runs on `ubuntu-latest`, downloads all six per-matrix asset artifacts, and runs the same checks as `release.yml` 4.5:
  - **Execution smoke test (linux/amd64)**: `chmod +x dist/explorer_${VERSION}_linux_amd64`, run `--version`, assert the output line equals `${VERSION} (commit ${SHORT_SHA}, built ${BUILD_DATE})` exactly. Then start the binary on `--port 18080` against `./testdata`, poll `GET /` until ready (`until curl -fsS http://127.0.0.1:18080/ ...; do sleep 0.2; done` with a 5s ceiling), assert HTTP 200 + `Content-Type: text/html` + non-empty body, send SIGTERM, assert exit 0 within graceful-shutdown timeout.
  - **Execution smoke test (linux/arm64)**: install `qemu-user-static` and run the same checks via `qemu-aarch64-static ./dist/explorer_${VERSION}_linux_arm64 ...`.
  - **Structural archive assertion (all six targets)**: list each archive's contents (`tar -tzf <archive> | sort` for `.tar.gz`, `unzip -l <archive>` for `.zip`) and assert exactly three top-level entries: `explorer[.exe]`, `LICENSE`, `README.md`.
- [x] 4a.6 Do NOT add a `release` job. PR builds end after `smoke-test`; the per-target asset artifacts uploaded by `build-binaries` are the workflow's only persistent output. Verify the workflow file with `actionlint` locally if available (`actionlint .github/workflows/pr-build.yml`); otherwise rely on GitHub's syntax check at push time.
- [x] 4a.7 Document the artifact-download flow informally (e.g. in a short release runbook section or PR description template — NOT in `README.md`, since this is a maintainer/contributor concern, not a user concern): "On a PR, click 'Checks' → 'pr-build' → scroll to 'Artifacts' → download the per-target zip you want to test." Note that artifacts from fork PRs require the first-time-contributor approval click (Settings → Actions → "Require approval for first-time contributors" is on by default). **Recorded in `docs/RELEASING.md`.**
- [x] 4a.8 (Repo configuration, NOT a code change — record in the release runbook so a future maintainer knows the rule exists) Set up branch protection on `main` requiring the `pr-build / smoke-test` check to pass before merge. This is a GitHub repository setting outside the workflow file; the spec cannot enforce it. Flag at the end of implementation so it is not forgotten before the first PR after merge. **Recorded in `docs/RELEASING.md`. Maintainer must apply manually.**

## 5. README and docs

- [x] 5.1 Update the existing "Install" section in `README.md` (currently lines 27–69, covering `go install`, `make build`, and Docker). Add **two new download subsections at the top** of the section — bare binary first (the simpler flow), archive second (for users who want bundled `LICENSE` + `README.md`) — then keep the existing `go install` / `make build` / Docker subsections below. Asset filenames are versioned (`explorer_<version>_<os>_<arch>[.ext]`); GitHub's `releases/latest/download/<filename>` URL only resolves if `<filename>` matches the latest release's exact asset name, which requires knowing the version. Use a literal `<TAG>` placeholder and direct the user to the [Releases page](https://github.com/anantadwi13/explorer/releases) for the current value. (The kubectl-style alias of unversioned filenames is rejected — see `design.md` Decision 5 — so we live with the placeholder.)
  - **Subsection 1: "Download the binary (fastest)."** Bare-binary flow, one curl + chmod, no extract:
    - linux/amd64: `curl -L -o explorer https://github.com/anantadwi13/explorer/releases/download/<TAG>/explorer_<TAG>_linux_amd64 && chmod +x explorer && ./explorer ./`
    - Mirror the snippet for linux/arm64, darwin/amd64, darwin/arm64.
    - Windows (PowerShell): `Invoke-WebRequest -Uri https://github.com/anantadwi13/explorer/releases/download/<TAG>/explorer_<TAG>_windows_amd64.exe -OutFile explorer.exe; .\explorer.exe .` (no `chmod` needed; the `.exe` extension is preserved by the bare-binary asset name).
  - **Subsection 2: "Download the archive (with LICENSE + README)."** Archive flow:
    - linux/amd64: `curl -L -o explorer.tar.gz https://github.com/anantadwi13/explorer/releases/download/<TAG>/explorer_<TAG>_linux_amd64.tar.gz && tar -xzf explorer.tar.gz && ./explorer ./` (executable bit preserved, no `chmod` needed).
    - Mirror for the other unix targets.
    - Windows (PowerShell): `Invoke-WebRequest` for the `.zip` + `Expand-Archive`.
  - **Subsection 3: "Verify the download."** `sha256sum --ignore-missing -c checksums.txt` (linux) and `shasum -a 256 --ignore-missing -c checksums.txt` (macOS). `--ignore-missing` is recommended so the verify step works for whichever subset of assets the user downloaded (bare binary alone, archive alone, or both).
  - **macOS Gatekeeper note** (one short paragraph after the verify subsection): `xattr -d com.apple.quarantine ./explorer` if the user sees the "cannot be opened" dialog. Applies to both the bare binary and the binary extracted from the archive.
  - **Windows SmartScreen note** (one short paragraph): click "More info → Run anyway". Applies to both shapes.
  - Keep the existing "With Go (recommended for users)", "From source (for contributors)", and "Docker / Docker Compose" subsections below the new download subsections. Reorder if the new download subsections read better as the lead.
- [x] 5.2 Mention the `--version` flag in the existing CLI/usage section of the README so users know how to verify what they have running. **Added a row to the Configuration table; the Verify subsection also references `./explorer --version` for cross-check.**
- [x] 5.3 Confirm the existing `go install` and `make build` instructions still work and are not contradicted by the new Install section. Reorder if the new section reads better last. **Existing sections kept verbatim; reorder placed download flows first, then Go install, then source build, then Docker. Drive-by: also fixed the now-stale "501 errors" line in the README troubleshooting section to match the actual 404 behaviour (consistent with §5a).**

## 5a. Documentation correction: empty-embed fallback is 404, not 501

The repo's existing documentation describes the empty-`internal/server/ui/dist/` fallback as a "501-style" response, but `internal/server/static.go:28` calls `http.ServeFileFS(w, r, dist, "index.html")` which returns the stock `http.NotFound` (HTTP 404, `Content-Type: text/plain; charset=utf-8`, body `404 page not found\n`). There is no 501 anywhere in `internal/` or `cmd/`. This change's smoke-test gate exposed the inaccuracy; fix the docs as part of this change so the new spec scenarios remain coherent with the rest of the repo.

- [x] 5a.1 Edit `CLAUDE.md` ("`make build` is required..." paragraph, around line 28). Replace `serves a `501`-style fallback if `internal/server/ui/dist/` is empty.` with `serves an HTTP 404 fallback (the stock `http.ServeFileFS` response when `dist/index.html` is missing) if `internal/server/ui/dist/` is empty.`. Confirm the rest of the paragraph still reads correctly.
- [x] 5a.2 Edit `openspec/specs/directory-browser/spec.md`. In the "Embedded SPA assets ship in the published module" requirement, the "Module-proxy install produces a runnable binary" scenario's last `**AND**` clause currently reads `the binary does NOT serve the "SPA not yet embedded" 501-style fallback`. Replace it with the wording from this change's `directory-browser` delta (which describes the actual 404 / `text/plain` response). After the canonical edit, this scenario MUST match the version in our delta verbatim — `/opsx:archive` will then re-apply cleanly without "drift" warnings.
- [x] 5a.3 Confirm no other source files reference "501" or `StatusNotImplemented` in this context (`grep -rn "501\|StatusNotImplemented" --include="*.go" --include="*.md" internal/ cmd/ openspec/specs/ CLAUDE.md`). The two docs above are the only canonical occurrences; archived changes under `openspec/changes/archive/` may reference 501 historically and SHOULD NOT be edited (archived artifacts are immutable history). **Verified clean — no matches.**
- [x] 5a.4 Run `make test` after the edits. No code changed, so no test should regress. **Pass.**

## 6. Specs sync

- [ ] 6.1 After implementation is complete and verified (sections 1–5a done — note §5a sits between §5 and §6, also a prerequisite), run `/opsx:verify` to confirm the implementation matches the change artifacts.
- [ ] 6.2 Run `/opsx:sync` (or `/opsx:archive`) to fold the delta from `specs/directory-browser/spec.md` into the canonical spec and add the new `release-distribution` capability under `openspec/specs/`.

## 7. First release

- [x] 7.1 Decide on the initial version (resolve the `v0.1.0` vs `v1.0.0` open question from `design.md`). **Decided 2026-05-03: `v0.1.0` — stay on `v0.x.x` until the surface stabilises. Updated 2026-05-04:** `v0.1.0` was already tagged at commit `2208398` (pre-dating this branch) without the release pipeline, so the first **prebuilt-binary** release is **`v0.2.0`** — minor bump because this change adds a new CLI flag (`--version`/`-v`) and a new distribution channel.
- [ ] 7.2 Tag the merged commit (`git tag -a v0.2.0 -m "Initial prebuilt binary release"`) and push the tag (`git push origin v0.2.0`).
- [ ] 7.3 Watch the workflow run; on success, verify the GitHub Release page shows twelve binary assets (six archives + six bare binaries) plus `checksums.txt`. Confirm asset names match the spec's `Matrix completeness` scenario verbatim.
- [ ] 7.4 Manually download and run the `darwin/arm64` (Apple Silicon) and `windows/amd64` binaries — the two targets the execution smoke test does not cover (their archives are still structurally checked in CI). For each target, test BOTH shapes: extract the archive and run `./explorer --version` + `./explorer ./`, then separately download the bare binary and run it directly to confirm `--version` + `./explorer ./` work without an extract step.
- [ ] 7.5 If anything is wrong, **bump to a new patch tag** (e.g. `v0.2.1`) rather than deleting and re-tagging `v0.2.0`. Tags that have been pushed are public — anyone who pulled the broken release would otherwise see silent content swap on `v0.2.0`. Mark the broken release as a pre-release / draft on GitHub if you want to hide it, but don't delete the tag itself.
