## 1. Fix README Startup Banner Example

- [x] 1.1 In `README.md` "Quick start" section, replace the incorrect startup banner code block with the correct output that matches `main.go`: add the missing playground-warning headline and remove the non-existent `(also reachable on your LAN)` annotation from the URL line
- [x] 1.2 Verify the updated example matches `cmd/explorer/main.go` line by line (lines 83-88)
